# PaperFlow 논문별 챗봇 구현 계획

> 📋 이 문서는 논문별 질문-답변 챗봇 기능의 전체 구현 계획입니다.
> 각 항목을 체크하면서 진행하세요.

---

## 📌 개요

**목표**: RAG 기반 논문별 질문-답변 챗봇을 PaperFlow 뷰어에 추가

**핵심 기능**:
- ✅ 논문별 질문-답변 (현재 논문 컨텍스트만 사용)
- ✅ 대화 기록 영구 저장 (세션 간 유지)
- ✅ 논문 간 완전 격리 (교차 오염 없음)
- ✅ 모바일/데스크톱 반응형 UI
- ✅ 기존 auto-hide 메커니즘과 통합

**확정된 구현 방식**:
- ✅ **RAG**: 키워드 검색 (BM25 스타일, 빠르고 간단, 의존성 최소)
- ✅ **버튼 표시**: 마크다운이 있는 논문만 (`hasMdKo || hasMdEn`)
- ✅ **모바일 동작**: 상단 바와 함께 숨김/표시 (`topBarVisible` 연동)
- ✅ **LLM 모델**: 기존 번역 API 설정 재사용 (gemini-2.5-pro)

---

## 🏗️ 아키텍처

### 데이터 흐름
```
사용자 질문 → FastAPI → RAG Service → LLM API (OpenAI 호환)
                ↓                           ↓
         chat_history.json ← ─ ─ ─ SSE Stream → Frontend
```

### 저장 구조
```
outputs/Paper Title/
  ├── Paper.md                # RAG 소스 (영문)
  ├── Paper_ko.md             # RAG 소스 (한글, 우선)
  ├── paper_meta.json
  ├── chat_history.json       # 🆕 대화 기록
  └── chat_chunks.json        # 🆕 청크 캐시
```

**chat_history.json 구조**:
```json
{
  "paper_name": "논문 제목",
  "messages": [
    {
      "role": "user",
      "content": "주요 기여는 무엇인가요?",
      "timestamp": "2026-02-07T10:30:00Z"
    },
    {
      "role": "assistant",
      "content": "이 논문의 주요 기여는...",
      "timestamp": "2026-02-07T10:30:15Z",
      "sources": [
        {
          "index": 0,
          "heading": "Abstract",
          "content": "This paper presents..."
        }
      ]
    }
  ],
  "created_at": "2026-02-07T10:30:00Z",
  "updated_at": "2026-02-07T10:30:15Z"
}
```

---

## 📝 구현 체크리스트

### Week 1: 백엔드 기초 (5일)

#### Day 1-2: 데이터 모델 및 채팅 기록 관리
- [ ] **파일 생성**: `viewer/app/models/chat.py`
  - [ ] `ChatMessage` 모델 정의 (role, content, timestamp, sources)
  - [ ] `ChatHistory` 모델 정의 (paper_name, messages, created_at, updated_at)
  - [ ] `ChatRequest` 모델 정의 (message, paper_name)
  - [ ] `ChatChunk` 모델 정의 (index, content, heading, start_line, end_line)

- [ ] **파일 생성**: `viewer/app/services/chat.py`
  - [ ] `load_chat_history(paper_name)` 함수 구현
    - [ ] `{paper_dir}/chat_history.json` 로드
    - [ ] 파일 없으면 빈 `ChatHistory` 반환
    - [ ] JSON 파싱 에러 처리
  - [ ] `save_chat_history(history)` 함수 구현
    - [ ] `updated_at` 타임스탬프 갱신
    - [ ] 최대 100개 메시지 제한 (오래된 메시지 제거)
    - [ ] JSON 파일로 저장 (indent=2, default=str)
  - [ ] `clear_chat_history(paper_name)` 함수 구현
    - [ ] `chat_history.json` 파일 삭제
    - [ ] 성공 여부 반환
  - [ ] `get_or_create_chunks(paper_name)` 함수 구현
    - [ ] 캐시 체크: `chat_chunks.json` 존재 시 로드
    - [ ] 캐시 없으면 `chunk_markdown()` 호출
    - [ ] 우선순위: `_ko.md` > `.md`
    - [ ] 생성된 청크를 캐시 파일에 저장

#### Day 3-4: RAG 파이프라인
- [ ] **파일 생성**: `viewer/app/services/rag.py`

- [ ] `chunk_markdown(md_path, chunk_size=500, overlap=50)` 구현
  - [ ] 마크다운 파일 읽기 (UTF-8)
  - [ ] 헤딩(`##`, `###`) 경계로 분할
  - [ ] 500토큰 청크 생성 (토큰 추정: `len(text) // 4`)
  - [ ] 50토큰 오버랩 적용
  - [ ] 코드 블록(`` ``` ``) 보존
  - [ ] 수식(`$$`, `$`) 보존
  - [ ] `ChatChunk` 객체 리스트 반환

- [ ] `search_chunks(query, chunks, top_k=5)` 구현 (키워드 검색)
  - [ ] 쿼리 토큰화 (소문자, 공백 분할)
  - [ ] 각 청크별 TF 스코어 계산
  - [ ] 헤딩 매치 시 10점 보너스
  - [ ] 스코어 기준 내림차순 정렬
  - [ ] 상위 `top_k`개 청크 반환

- [ ] `build_rag_context(query, chunks, history, max_chunks=5, max_history=2)` 구현
  - [ ] "# Paper Content (Relevant Excerpts)" 섹션 생성
  - [ ] 각 청크별 헤딩과 내용 포함
  - [ ] "# Previous Conversation" 섹션 생성 (최근 2턴)
  - [ ] "# Current Question" 섹션 생성
  - [ ] 전체 컨텍스트 문자열 반환

- [ ] `async generate_response_stream(context, query, model, base_url, api_key)` 구현
  - [ ] `AsyncOpenAI` 클라이언트 생성
  - [ ] 시스템 프롬프트 정의 ("학술 논문 이해를 돕는 연구 보조 AI")
  - [ ] `chat.completions.create()` 호출 (stream=True)
  - [ ] 스트리밍 응답 yield
    - [ ] `{"type": "token", "content": "..."}` 형식
    - [ ] 완료 시 `{"type": "done"}` 전송
  - [ ] 에러 발생 시 `{"type": "error", "error": "..."}` 전송

#### Day 5: API 엔드포인트 (SSE)
- [ ] **파일 수정**: `viewer/app/routers/api.py` (Line 172 이후)

- [ ] 임포트 추가
  - [ ] `from sse_starlette.sse import EventSourceResponse`
  - [ ] `from ..services import chat as chat_svc`
  - [ ] `from ..services import rag as rag_svc`
  - [ ] `from ..models.chat import ChatRequest, ChatMessage`

- [ ] `POST /api/papers/{name:path}/chat` 엔드포인트 구현
  - [ ] `name` URL 디코딩 (`unquote()`)
  - [ ] 인증 체크 (`Depends(get_current_user_api)`)
  - [ ] `async def event_generator()` 정의:
    - [ ] 1. 채팅 기록 로드 (`chat_svc.load_chat_history()`)
    - [ ] 2. 사용자 메시지 추가
    - [ ] 3. 청크 가져오기 (`chat_svc.get_or_create_chunks()`)
    - [ ] 4. 관련 청크 검색 (`rag_svc.search_chunks()`)
    - [ ] 5. RAG 컨텍스트 구축 (`rag_svc.build_rag_context()`)
    - [ ] 6. LLM 응답 스트리밍 (`rag_svc.generate_response_stream()`)
    - [ ] 7. 응답 누적 및 실시간 전송
    - [ ] 8. 소스 전송 (`{"type": "sources", "sources": [...]}`))
    - [ ] 9. 어시스턴트 메시지 저장
    - [ ] 10. 완료 이벤트 전송 (`{"type": "done"}`)
  - [ ] `EventSourceResponse` 반환
  - [ ] 에러 핸들링 (try-except)

- [ ] `GET /api/papers/{name:path}/chat/history` 엔드포인트 구현
  - [ ] 채팅 기록 로드
  - [ ] JSON 반환
  - [ ] 404 에러 처리 (논문 없음)

- [ ] `DELETE /api/papers/{name:path}/chat/history` 엔드포인트 구현
  - [ ] 채팅 기록 삭제
  - [ ] 성공 메시지 반환
  - [ ] 404 에러 처리

---

### Week 2: 프론트엔드 UI (5일)

#### Day 1-2: 챗봇 버튼 및 패널 HTML/CSS
- [ ] **파일 수정**: `viewer/app/templates/viewer.html` (Line 589 이후)

- [ ] 챗봇 버튼 HTML 추가
  - [ ] Alpine.js `x-data="chatButton()"` 컴포넌트
  - [ ] 조건부 표시: `x-show="hasMdKo || hasMdEn"`
  - [ ] 위치: `fixed bottom-6 right-6`
  - [ ] 모바일 동작:
    - [ ] `topBarVisible` true → 표시
    - [ ] `topBarVisible` false → 숨김 (opacity-0, pointer-events-none)
  - [ ] 챗봇 아이콘 SVG (말풍선)
  - [ ] 버튼 스타일: `bg-indigo-600 hover:bg-indigo-700`
  - [ ] 크기: `w-14 h-14 rounded-full`
  - [ ] 그림자: `shadow-lg`

- [ ] 챗봇 패널 HTML 추가
  - [ ] Alpine.js `x-data="chatPanel()"` 컴포넌트
  - [ ] 조건부 표시: `x-show="chatPanelOpen"`
  - [ ] 위치 및 크기:
    - [ ] 모바일: `inset-0` (전체 화면)
    - [ ] 데스크톱: `bottom-0 right-0 w-96 h-[600px] m-6 mr-24 rounded-tl-2xl`
  - [ ] 슬라이드 애니메이션: `x-transition` (우측에서 좌측으로)
  - [ ] 클릭 외부 닫기: `@click.away="closeChat()"`
  - [ ] ESC 키 닫기: `@keydown.escape.window="closeChat()"`
  - [ ] ARIA 속성: `role="dialog"`, `aria-modal="true"`

- [ ] 패널 헤더
  - [ ] 제목: "Paper Chat"
  - [ ] 아이콘: 채팅 아이콘
  - [ ] 버튼들:
    - [ ] 대화 기록 삭제 버튼 (`@click="confirmClearHistory()"`)
    - [ ] 닫기 버튼 (`@click="closeChat()"`)
  - [ ] 다크모드 대응: `$store.darkMode.on` 조건부 스타일

- [ ] 메시지 영역
  - [ ] 스크롤 가능 영역: `overflow-y-auto`
  - [ ] ref 참조: `x-ref="messagesContainer"`
  - [ ] 환영 메시지 (메시지 없을 때): `x-show="messages.length === 0"`
  - [ ] 메시지 루프: `x-for="(msg, idx) in messages"`
    - [ ] 사용자 메시지: 우측 정렬 (`justify-end`)
    - [ ] 어시스턴트 메시지: 좌측 정렬 (`justify-start`)
    - [ ] 메시지 버블 스타일:
      - [ ] 사용자: `bg-indigo-600 text-white rounded-br-none`
      - [ ] 어시스턴트: `bg-white border rounded-bl-none` (라이트) / `bg-gray-800 border-gray-700` (다크)
    - [ ] 타임스탬프 표시: `formatTime(msg.timestamp)`
    - [ ] 소스 토글 버튼 (어시스턴트 메시지만)
    - [ ] 소스 세부정보 (접을 수 있는): `x-show="showSources[idx]"` `x-collapse`
  - [ ] 스트리밍 인디케이터:
    - [ ] 표시 조건: `x-show="isStreaming"`
    - [ ] 애니메이션: 3개 점 bounce
    - [ ] 텍스트: "Thinking..."

- [ ] 입력 영역
  - [ ] Form: `@submit.prevent="sendMessage()"`
  - [ ] Input:
    - [ ] `x-model="userInput"`
    - [ ] placeholder: "Ask about this paper..."
    - [ ] disabled: `:disabled="isStreaming"`
    - [ ] 다크모드 대응 스타일
  - [ ] 전송 버튼:
    - [ ] 아이콘: 화살표 (➤)
    - [ ] disabled: `:disabled="!userInput.trim() || isStreaming"`
    - [ ] 스타일: `bg-indigo-600` (활성) / `bg-gray-400 cursor-not-allowed` (비활성)

#### Day 3-4: JavaScript 로직
- [ ] **파일 수정**: `viewer/app/templates/viewer.html` (Script 섹션, Line 1385 이후)

- [ ] `chatButton()` 함수 구현
  - [ ] 상태: `chatPanelOpen`
  - [ ] `init()`:
    - [ ] `chat-panel-opened` 이벤트 리스너
    - [ ] `chat-panel-closed` 이벤트 리스너
  - [ ] `isMobile()`: `window.innerWidth < 768`
  - [ ] `openChat()`: `open-chat-panel` 이벤트 디스패치

- [ ] `chatPanel()` 함수 구현
  - [ ] 상수: `paperName = '{{ paper_name_encoded }}'`
  - [ ] 상태 변수:
    - [ ] `chatPanelOpen: false`
    - [ ] `messages: []`
    - [ ] `userInput: ''`
    - [ ] `isStreaming: false`
    - [ ] `showSources: {}`

- [ ] `init()` 메서드
  - [ ] `open-chat-panel` 이벤트 리스너 등록
  - [ ] `loadHistory()` 호출 (초기 대화 기록 로드)

- [ ] `openPanel()` 메서드
  - [ ] `chatPanelOpen = true`
  - [ ] `chat-panel-opened` 이벤트 디스패치
  - [ ] 모바일일 경우:
    - [ ] `viewerApp` Alpine 데이터 가져오기
    - [ ] `ensureTopBarVisible()` 호출
  - [ ] `$nextTick()`:
    - [ ] Input 포커스
    - [ ] `scrollToBottom()` 호출

- [ ] `closeChat()` 메서드
  - [ ] `chatPanelOpen = false`
  - [ ] `chat-panel-closed` 이벤트 디스패치

- [ ] `loadHistory()` 메서드 (async)
  - [ ] API 호출: `GET /api/papers/${paperName}/chat/history`
  - [ ] 응답 파싱
  - [ ] `messages` 배열 업데이트
  - [ ] `scrollToBottom()` 호출

- [ ] `sendMessage()` 메서드 (async)
  - [ ] 입력 검증 (`userInput.trim()`)
  - [ ] 스트리밍 중 방지 (`isStreaming` 체크)
  - [ ] 사용자 메시지 추가:
    - [ ] `{ role: 'user', content: message, timestamp: new Date().toISOString() }`
    - [ ] `messages` 배열에 push
  - [ ] `userInput` 초기화
  - [ ] `isStreaming = true`
  - [ ] `scrollToBottom()` 호출
  - [ ] **SSE 스트리밍 처리**:
    - [ ] `fetch()` 호출: `POST /api/papers/${paperName}/chat`
    - [ ] `ReadableStream` reader 생성
    - [ ] 어시스턴트 메시지 객체 생성 (빈 content)
    - [ ] `messages`에 추가
    - [ ] While 루프:
      - [ ] `reader.read()` 호출
      - [ ] 청크 디코딩 (`TextDecoder`)
      - [ ] 라인별 파싱 (`data: ` 접두사)
      - [ ] JSON 파싱
      - [ ] 이벤트 타입별 처리:
        - [ ] `token`: `content` 누적, `scrollToBottom()` 호출
        - [ ] `sources`: `sources` 배열 저장
        - [ ] `done`: 루프 종료
    - [ ] 에러 처리:
      - [ ] `showToast('Failed to send message', 'error')`
      - [ ] 마지막 메시지 제거 (`messages.pop()`)
    - [ ] Finally:
      - [ ] `isStreaming = false`

- [ ] `confirmClearHistory()` 메서드 (async)
  - [ ] 확인 다이얼로그: `confirm('Clear all chat history?')`
  - [ ] API 호출: `DELETE /api/papers/${paperName}/chat/history`
  - [ ] `messages = []` 초기화
  - [ ] `showSources = {}` 초기화
  - [ ] `showToast('Chat history cleared', 'success')`

- [ ] `toggleSources(idx)` 메서드
  - [ ] `showSources[idx]` 토글

- [ ] `scrollToBottom()` 메서드
  - [ ] `messagesContainer` ref 가져오기
  - [ ] `scrollTop = scrollHeight` 설정

- [ ] `formatTime(timestamp)` 메서드
  - [ ] `Date` 객체 생성
  - [ ] `toLocaleTimeString()` 호출 (hour, minute)
  - [ ] 에러 처리 (빈 문자열 반환)

- [ ] `renderMarkdown(text)` 메서드 (선택사항)
  - [ ] `marked.parse()` 사용 (있으면)
  - [ ] 없으면 `\n` → `<br>` 변환

#### Day 5: 모바일 테스트 및 통합
- [ ] **모바일 반응형 테스트**
  - [ ] 브라우저 개발자 도구 모바일 뷰 (< 768px)
  - [ ] 챗봇 버튼 숨김/표시 동작 확인
    - [ ] 화면 터치 → 버튼 표시
    - [ ] 스크롤/타임아웃 → 버튼 숨김
  - [ ] 챗봇 패널 전체화면 확인
  - [ ] 키보드 오버랩 테스트
  - [ ] 터치 제스처 테스트

- [ ] **데스크톱 테스트**
  - [ ] 챗봇 버튼 항상 표시 확인
  - [ ] 패널 크기 확인 (`w-96 h-[600px]`)
  - [ ] 드래그 불가능 확인 (고정 위치)

- [ ] **다크모드 테스트**
  - [ ] 버튼 스타일 전환 확인
  - [ ] 패널 스타일 전환 확인
  - [ ] 메시지 버블 스타일 전환 확인

- [ ] **접근성 테스트**
  - [ ] 키보드 탐색 (Tab, Enter, Escape)
  - [ ] ARIA 속성 확인
  - [ ] 스크린 리더 호환성 (선택사항)

---

### Week 3: 통합 및 테스트 (5일)

#### Day 1-2: 백엔드 통합 및 논문 관리 연동
- [ ] **파일 수정**: `viewer/app/services/papers.py`

- [ ] `delete_paper()` 함수 수정 (Line 201)
  - [ ] 챗봇 파일 정리 로직 추가:
    - [ ] `chat_history.json` 삭제
    - [ ] `chat_chunks.json` 삭제
  - [ ] 에러 처리 (파일 없어도 계속 진행)

- [ ] `_paper_info()` 함수 수정 (Line 31)
  - [ ] `chat_history.json` 존재 여부 확인
  - [ ] 메시지 개수 계산
  - [ ] `info["chat"]` 객체 추가:
    - [ ] `has_history: bool`
    - [ ] `message_count: int`

- [ ] **의존성 추가**: `viewer/requirements.txt`
  - [ ] `sse-starlette==2.1.0` 추가

- [ ] **의존성 설치 테스트**
  - [ ] `pip install -r viewer/requirements.txt` 실행
  - [ ] 임포트 에러 확인

#### Day 3: 전체 통합 테스트 (로컬)
- [ ] **백엔드 단위 테스트**
  - [ ] `chunk_markdown()` 함수 테스트
    - [ ] 500토큰 청크 확인
    - [ ] 오버랩 확인
    - [ ] 헤딩 경계 확인
  - [ ] `search_chunks()` 함수 테스트
    - [ ] 키워드 매칭 확인
    - [ ] 스코어링 확인
    - [ ] top_k 확인
  - [ ] `build_rag_context()` 함수 테스트
    - [ ] 컨텍스트 형식 확인
    - [ ] 히스토리 포함 확인

- [ ] **API 엔드포인트 테스트**
  - [ ] `POST /api/papers/{name}/chat` (curl 또는 Postman)
    - [ ] SSE 스트리밍 확인
    - [ ] 토큰 수신 확인
    - [ ] 소스 수신 확인
    - [ ] 완료 이벤트 확인
  - [ ] `GET /api/papers/{name}/chat/history`
    - [ ] JSON 형식 확인
    - [ ] 메시지 배열 확인
  - [ ] `DELETE /api/papers/{name}/chat/history`
    - [ ] 파일 삭제 확인
    - [ ] 성공 메시지 확인

- [ ] **프론트엔드 통합 테스트**
  - [ ] 뷰어 페이지 열기
  - [ ] 마크다운 있는 논문 확인
  - [ ] 챗봇 버튼 표시 확인
  - [ ] 버튼 클릭 → 패널 열림 확인
  - [ ] 질문 입력 → 전송 확인
  - [ ] 스트리밍 응답 확인
  - [ ] 타임스탬프 확인
  - [ ] 소스 토글 확인
  - [ ] 대화 기록 삭제 확인
  - [ ] 패널 닫기 확인

- [ ] **엔드투엔드 시나리오 1: 첫 대화**
  - [ ] 1. 뷰어에서 논문 열기
  - [ ] 2. 챗봇 버튼 클릭
  - [ ] 3. "What is the abstract?" 질문
  - [ ] 4. 스트리밍 응답 확인
  - [ ] 5. Abstract 섹션 인용 확인
  - [ ] 6. 타임스탬프 확인
  - [ ] 7. 패널 닫기

- [ ] **엔드투엔드 시나리오 2: 연속 대화**
  - [ ] 1. 챗봇 재오픈
  - [ ] 2. 이전 대화 로드 확인
  - [ ] 3. "Tell me more about the methodology" 질문
  - [ ] 4. 컨텍스트 유지 확인 (이전 대화 참조)
  - [ ] 5. 응답 확인

- [ ] **엔드투엔드 시나리오 3: 세션 지속성**
  - [ ] 1. 페이지 새로고침
  - [ ] 2. 챗봇 열기
  - [ ] 3. 이전 대화 기록 유지 확인
  - [ ] 4. 대화 기록 삭제
  - [ ] 5. 빈 상태 확인 ("Ask me anything...")

- [ ] **엔드투엔드 시나리오 4: 논문 삭제**
  - [ ] 1. 대화 기록이 있는 논문 삭제
  - [ ] 2. `outputs/` 디렉토리에서 폴더 삭제 확인
  - [ ] 3. `chat_history.json` 삭제 확인
  - [ ] 4. `chat_chunks.json` 삭제 확인

#### Day 4: Docker Compose 테스트
- [ ] **Docker 환경 설정**
  - [ ] `.env` 파일 확인
    - [ ] `OPENAI_BASE_URL` 설정 확인
    - [ ] `OPENAI_API_KEY` 설정 확인
  - [ ] 볼륨 마운트 확인 (`docker-compose.yml`)
    - [ ] `./outputs:/data/outputs`
    - [ ] `./archives:/data/archives`

- [ ] **Docker 이미지 빌드**
  - [ ] `docker compose build viewer`
  - [ ] 빌드 에러 확인
  - [ ] 의존성 설치 확인 (`sse-starlette`)

- [ ] **Docker 컨테이너 시작**
  - [ ] `docker compose up -d`
  - [ ] 컨테이너 상태 확인: `docker compose ps`
  - [ ] 로그 확인: `docker compose logs -f viewer`

- [ ] **Docker 환경 테스트**
  - [ ] `http://localhost:8000` 접속
  - [ ] 로그인
  - [ ] 논문 뷰어 열기
  - [ ] 챗봇 기능 전체 테스트 (위 시나리오 반복)
  - [ ] 볼륨 마운트 확인:
    - [ ] `outputs/` 디렉토리에 `chat_history.json` 생성 확인
    - [ ] 호스트에서 파일 확인

- [ ] **Docker 재시작 테스트**
  - [ ] `docker compose restart viewer`
  - [ ] 대화 기록 유지 확인
  - [ ] 청크 캐시 유지 확인

#### Day 5: 버그 수정 및 최종 검증
- [ ] **알려진 엣지 케이스 테스트**
  - [ ] 마크다운 없는 논문:
    - [ ] 챗봇 버튼 숨김 확인
  - [ ] 빈 대화:
    - [ ] 환영 메시지 표시 확인
  - [ ] 긴 논문 (200+ 청크):
    - [ ] 청크 생성 시간 측정 (<5초)
    - [ ] 검색 속도 측정 (<100ms)
  - [ ] API 타임아웃:
    - [ ] 60초 타임아웃 설정
    - [ ] 에러 메시지 표시 확인
  - [ ] 스트리밍 중단:
    - [ ] 네트워크 끊김 시뮬레이션
    - [ ] "Connection lost" 에러 확인
  - [ ] 모바일 키보드 오버랩:
    - [ ] 입력창 스크롤 확인
  - [ ] 논문 간 격리:
    - [ ] 다른 논문 열기
    - [ ] 대화 기록 분리 확인

- [ ] **성능 테스트**
  - [ ] 청크 캐싱:
    - [ ] 첫 번째 질문: 청크 생성 시간 측정
    - [ ] 두 번째 질문: 캐시 로드 시간 측정 (<50ms)
  - [ ] 키워드 검색:
    - [ ] 100 청크 검색 시간 측정 (<100ms)
  - [ ] SSE 스트리밍:
    - [ ] 토큰당 지연 시간 측정 (~5-10ms)
  - [ ] 전체 응답 시간:
    - [ ] 질문 → 첫 토큰: <1초
    - [ ] 전체 응답: <5초

- [ ] **보안 체크**
  - [ ] 입력 길이 제한 (1000자) 확인
  - [ ] SQL Injection 방지 (N/A - 파일 기반)
  - [ ] XSS 방지 (HTML 이스케이프 확인)
  - [ ] CSRF 방지 (기존 JWT 토큰 사용)

- [ ] **발견된 버그 수정**
  - [ ] 버그 목록 작성
  - [ ] 우선순위 결정
  - [ ] 수정 및 재테스트

---

## ✅ 검증 체크리스트

### 기능 검증
- [ ] ✅ 사용자가 논문에 대해 질문할 수 있다
- [ ] ✅ 답변이 논문 섹션을 인용한다 (sources 배열)
- [ ] ✅ 대화 기록이 세션 간 유지된다 (chat_history.json)
- [ ] ✅ 모바일에서 상단 바와 함께 숨김/표시된다
- [ ] ✅ 데스크톱에서 우측 하단에 고정된다
- [ ] ✅ 논문 삭제 시 채팅 기록도 삭제된다
- [ ] ✅ 응답 지연 시간 <5초
- [ ] ✅ Docker Compose 환경에서 작동한다
- [ ] ✅ 키보드 탐색 가능하다 (Tab, Enter, Escape)

### 품질 검증
- [ ] ✅ 코드 스타일 일관성 (기존 PaperFlow 패턴 준수)
- [ ] ✅ 에러 핸들링 완전성
- [ ] ✅ 로딩 상태 표시 (isStreaming)
- [ ] ✅ 사용자 피드백 (Toast 메시지)
- [ ] ✅ 다크모드 지원
- [ ] ✅ 모바일 반응형
- [ ] ✅ 접근성 (ARIA 속성)

### 성능 검증
- [ ] ✅ 청크 캐싱 작동 (두 번째 질문 빠름)
- [ ] ✅ 키워드 검색 <100ms
- [ ] ✅ SSE 스트리밍 안정적
- [ ] ✅ 메모리 누수 없음
- [ ] ✅ 긴 대화 기록 처리 (100 메시지 제한)

---

## 📚 참고 자료

### 파일 경로 참조
- **백엔드**:
  - API 라우터: `viewer/app/routers/api.py`
  - 서비스 레이어: `viewer/app/services/`
  - 모델: `viewer/app/models/`
  - 설정: `viewer/app/config.py`
  - 인증: `viewer/app/auth.py`

- **프론트엔드**:
  - 베이스 템플릿: `viewer/app/templates/base.html`
  - 뷰어 템플릿: `viewer/app/templates/viewer.html`
  - 논문 목록: `viewer/app/templates/papers.html`

- **설정**:
  - 환경 변수: `.env`
  - 파이프라인 설정: `config.json`
  - Docker: `docker-compose.yml`
  - 요구사항: `viewer/requirements.txt`

### 핵심 패턴
- **API 엔드포인트**: `unquote(name)`, `Depends(get_current_user_api)`, HTTPException
- **서비스 레이어**: `tuple[bool, str]` 반환, shutil 사용
- **전역 함수**: `apiFetch()`, `showToast()` (base.html)
- **Alpine.js**: `x-data`, `x-show`, `x-model`, `@click`, `$nextTick`
- **SSE**: `EventSourceResponse`, `async def generator()`, `yield {}`

---

## 🎯 성공 기준

이 모든 항목이 체크되면 챗봇 기능이 성공적으로 구현된 것입니다:

1. ✅ **기능 완성도**: 모든 핵심 기능이 작동한다
2. ✅ **품질**: 코드가 깨끗하고 유지보수 가능하다
3. ✅ **성능**: 응답 시간이 기대치를 충족한다
4. ✅ **사용성**: UI/UX가 직관적이고 반응성이 좋다
5. ✅ **안정성**: 에러 처리가 완전하고 엣지 케이스를 커버한다
6. ✅ **통합**: 기존 PaperFlow 기능과 잘 통합된다
7. ✅ **배포**: Docker 환경에서 문제없이 작동한다

---

**작성일**: 2026-02-07
**작성자**: Claude Code
**버전**: 1.0
**상태**: 구현 준비 완료 ✅
